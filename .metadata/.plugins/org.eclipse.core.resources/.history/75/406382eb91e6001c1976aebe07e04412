package jiraAPITest;

import io.restassured.RestAssured;
import io.restassured.path.json.JsonPath;

import static io.restassured.RestAssured.*;

public class JiraTest {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		RestAssured.baseURI="http://localhost:6060";
		
		//********LOGIN********//
		
		String login_Response = given().log().all().header("Content-Type","application/json")
				.body(JiraPayloads.LoginPayload())
		.when().post("/rest/auth/1/session")
		.then().log().all().assertThat().statusCode(200).extract().response().asString(); 
		
		/*//Session filter class can be used to record session before when()
		 * JsonPath js = new JsonPath(login_Response); String name =
		 * js.getString("session.name"); String value = js.getString("session.value");
		 * String cookie = name+value; System.out.println("**************"+cookie);
		 */
		
		
 
 
	}

}
